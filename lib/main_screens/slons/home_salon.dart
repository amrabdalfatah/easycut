import 'package:flutter/material.dart';

class HomeSalon extends StatelessWidget {
  const HomeSalon({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Salon Home'),
      ),
    );
  }
}
